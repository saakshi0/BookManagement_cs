@* @model List<BooksEntity>

@{
    ViewData["Title"] = "Books";
}

<h2>Books</h2>

<br>

<div class="row">
    <div class="col-md-12">
        <a asp-action="Create" asp-asp-controller="=Book" class="btn btn--success">Create Book</a>
    </div>
</div>

<div class="row">
    <div class="col-lg-12">
        <table>
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>ISBN</th>
                    <th colspan="2">Actions</th>

                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Author</td>
                        <td>@item.ISBN</td>
                        <td>
                            <a asp-action="Edit" asp-route-id="@item.Id">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div> *@


@model List<BooksEntity>

@{
    ViewData["Title"] = "Books";
}

<h2 class="text-center mt-4">@ViewData["Title"]</h2>

<div class="row mt-4">
    <div class="col-lg-12">
        <table class="table table-striped table-bordered">
            <thead class="thead-dark">
                <tr>
                    <th>Title</th>
                    <th>Author</th>
                    <th>ISBN</th>
                    <th class="text-center">Actions</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>@item.Title</td>
                        <td>@item.Author</td>
                        <td>@item.ISBN</td>
                        <td class="text-center">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-primary">Edit</a>
                            <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Delete</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<!-- Fixed 'Create Book' button at the bottom-right -->
<div style="position:sticky; bottom: 20px; right: 20px;">
    <a asp-action="Create" asp-controller="Book" class="btn btn-success btn-lg">
        Create Book
    </a>
</div>
